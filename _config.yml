title: GenBasic
tagline: Electronics for commercial deployment
description: >-
  GenBasic builds wireless adapters and power supplies for commercial and
  embedded use — tightly specified, tested together, and sold under stable
  model numbers.
url: https://genbasic.com
baseurl: ""

# Built by GitHub Pages' native Jekyll. No plugins beyond the Pages default set,
# and no remote theme: the layout in _layouts/ is self-contained so the build has
# nothing to resolve at publish time.
theme:
plugins: []

exclude:
  - README.md
  - LICENSE
